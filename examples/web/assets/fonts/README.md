Put `Milling-Duplex1mm.woff2` (the ai-coustics brand typeface, licensed from
205TF) in this folder to get the display font. It is gitignored on purpose; the
page falls back to Hanken Grotesk when it is missing.
